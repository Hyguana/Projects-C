#include <stdio.h>
#include <stdlib.h>
#include <conio.h>
main()
{
	int i,n;
	float somatorio;
	printf("\n\t Por favor inserir um número para Somatorio: ");
	scanf("%d", &n);
	somatorio=0.0
	for(i=1;i<=n;i++)
	{
		somatorio=somarotio+(1.0/i);
	}
	printf("Somatório = %s", somatorio);
}